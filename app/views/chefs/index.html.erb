<div class="container">
    <div class="row">
        <div class="col-md-12">
            <ul>
                <% @chefs.each do |chef| %>
                    <li><h1>Chef: <%= link_to chef.chefname, chef_path(chef) %></h1></li>    
                <% end %>
            </ul>

        </div>
    </div>
</div>